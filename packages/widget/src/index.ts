export { AssistWidget } from "./assist-widget.js";

// Convenience for React hosts (they can import and use the custom element)
export function defineAssistWidget() {
  // already defined in module
}
